#include<graphics.h>
#include<conio.h>
void main()
{
	initgraph(350, 600);
	IMAGE img_bk; //定义IMAGE对象
	loadimage(&img_bk, _T("F:\\backgroundfd.jpg")); //读取图片到img对象中
	putimage(0, 0, &img_bk);
	_getch();
	关闭图（）；//结束
}
